apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-app.fullname" . }}-frontend
  labels:
    {{- include "ai-app.frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
  - port: {{ .Values.frontend.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "ai-app.frontend.selectorLabels" . | nindent 4 }}
