# Production environment
global:
  environment: production

# Azure PostgreSQL Flexible Server configuration (Production)
postgresql:
  # Azure PostgreSQL Flexible Server hostname
  host: "psql-isaru66-aiapp-asse-001.postgres.database.azure.com"
  port: 5432
  database: "chatdb"
  # Azure requires SSL/TLS for connections
  sslMode: "require"
  auth:
    username: "chatapp"
    password: "" # REQUIRED - provide via Azure Key Vault or --set

backend:
  replicaCount: 3
  image:
    tag: "v1.0.0"

frontend:
  replicaCount: 3
  image:
    tag: "v1.0.0"

gateway:
  tls:
    enabled: true
# Azure Workload Identity (set enabled: true to use)
azure:
  workloadIdentity:
    enabled: false
    tenantId: ""
    clientId: ""